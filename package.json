{
  "name": "catstar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/node/*.test.js",
    "lint": "biome check scripts tests/node",
    "lint:write": "biome check --write scripts tests/node",
    "format": "biome format --write scripts tests/node"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4"
  }
}
